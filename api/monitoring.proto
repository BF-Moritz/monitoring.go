syntax = "proto3";

package monitoring;

option go_package = "github.com/BF-Moritz/monitoring.go/api";

message Ping {
    uint64 id = 1;
    string client = 2;
    string body = 3;
}

message Pong {
    uint64 id = 1;
}

service ChatService{
    rpc SayHello(Ping) returns (Pong) {}
}